import DevProjects from "./DevProjects";

// Este componente ahora es un wrapper sobre DevProjects para mantener compatibilidad
const ProjectGrid = () => {
  return <DevProjects />;
};

export default ProjectGrid;
